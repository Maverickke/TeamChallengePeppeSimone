//file .h contengono solo le dichiarazioni. tramite #include ci accediamo. usarlo solamente per le cose che ci servono.
//#include"" relativa alla directory in cui si trova la funzione.
//#include<>  

//void hello(void);
#include "dumb.h"

int number = 17; //Nello stack e nello heap le variabili non vengono inizializzate.


//extern standard input output error =>

int boh(int a, int b)
{
    hello();
    return a + b;
}

